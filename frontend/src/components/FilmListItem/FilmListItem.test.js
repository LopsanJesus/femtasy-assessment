import React from "react";
import { shallow } from "enzyme";
import FilmListItem from "./FilmListItem";

describe("FilmListItem", () => {
    test("matches snapshot", () => {
        // const wrapper = shallow(<FilmListItem />);
        // expect(wrapper).toMatchSnapshot();
    });
});
